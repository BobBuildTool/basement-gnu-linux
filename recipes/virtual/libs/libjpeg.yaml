multiPackage:
    dev:
        depends:
            - name: ${CONFIG_SELECT_LIBJPEG:-libs::jpeg-turbo}-dev
              use: []

    tgt:
        depends:
            - name: ${CONFIG_SELECT_LIBJPEG:-libs::jpeg-turbo}-tgt
              use: []

provideDeps: [ "*" ]
